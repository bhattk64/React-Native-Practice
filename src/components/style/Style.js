import { StyleSheet } from "react-native";

const styles=StyleSheet.create({
    text:{
        fontSize:20,
        color:'red',
        fontWeight:'bold',
        marginVertical:10,
        padding:5,
        borderWidth:4
    }
})

export default styles